from random import randint

def minu_shuffle(järjend):
    järjend = [1,2,3,4,5,6]
    list = []
    while len(järjend) > 0:
        järjekord = random.randrange(0,len(järjend,6))
        uus_list = list.append(järjend.pop.(järjekord))
        return result